import React from "react";
import ExploreIcon from "../../../assets/Icons/Explore.svg";

const Explore = props => {
  return <ExploreIcon width={props.width} height={props.height} />;
};

export { Explore };
