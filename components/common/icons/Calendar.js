import React from "react";
import CalendarIcon from "../../../assets/Icons/Calendar.svg";

const Calendar = props => {
  return <CalendarIcon width={props.width} height={props.height} />;
};

export { Calendar };
