import React from "react";
import ProfileIcon from "../../../assets/Icons/Profile.svg";

const Profile = props => {
  return <ProfileIcon width={props.width} height={props.height} />;
};

export { Profile };
