import React from "react";
import SportsIcon from "../../../assets/Icons/Sports.svg";

const Sports = props => {
  return <SportsIcon width={props.width} height={props.height} />;
};

export { Sports };
